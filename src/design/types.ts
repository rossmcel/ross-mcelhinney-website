/* eslint-disable no-alert, no-unused-vars */
/* eslint-disable no-alert, no-shadow */
// to use: import this file, and pass a prop to a component like so: topMargin = {sizes.miniMargin}
export enum Sizes {
  BASE,
  XSMALL,
  SMALL,
  MEDIUM,
  LARGE,
  XL,
}
